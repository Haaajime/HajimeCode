# 开发日志（Agent Harness 项目）

> 追加式记录。每完成一项工作在此追加一条 [日期] / 内容 / 改动文件 / 测试结果 / commit。
> 与 `开发规范.md` 配合，遵循"完成一项 → 记日志 → git commit"。

---

## 2026-09-15 · 里程碑 0：项目初始化与工程建立

- **内容**：使用 `uv` 初始化项目，确定 Python 版本与依赖管理方式；建立敏感信息与日志/规范文档体系。
- **改动文件**：
  - `pyproject.toml`（uv 建包，name=agent-harness）
  - `.python-version`（钉到 3.10，规避网络下载 3.11 超时）
  - `.env`（含 DeepSeek key，git 已忽略）、`.env.example`（占位）
  - `.gitignore`（加入 `.env` 忽略规则）
  - `docs/开发规范.md`、`docs/开发日志.md`
- **测试结果**：无（纯工程搭建）。
- **commit**：`chore(init): 项目初始化,树立开发规范与日志体系`

## 2026-09-15 · 里程碑 1：核心引擎（循环 + 工具注册表 + Provider）

- **内容**：实现 Agent 主循环、工具分发注册表、模型 Provider 抽象（DeepSeek OpenAI 兼容）与 CLI 入口。
- **改动文件**：
  - `src/agent_harness/config.py`（.env 配置加载）
  - `src/agent_harness/providers.py`（OpenAICompatibleProvider）
  - `src/agent_harness/tools.py`（Tool + ToolRegistry + 内置 add/echo）
  - `src/agent_harness/loop.py`（agent_loop：tool_use 分发 + tool_result 回填 + 步数上限）
  - `src/agent_harness/cli.py`、`src/agent_harness/__init__.py`、`pyproject.toml`（脚本入口）
  - `tests/test_loop.py`（5 条 mock 单测）
  - `README.md`
- **测试结果**：`uv run pytest -q` → **5 passed**（全部 mock，零 API 消耗）。
- **真实 API 冒烟**：一次极小 token 调用 DeepSeek（max_tokens=120）返回"联通"，端到端打通。
- **commit**：`feat(core): 实现 Agent 主循环、工具注册表与 DeepSeek Provider`

## 2026-09-15 · 里程碑 2：权限治理层（s03）

- **内容**：新增权限判定层,在工具执行前做 `allow/ask/deny` 判定,默认 fail-closed(未命中放行规则即拒绝),并接入主循环:拒绝以 `[permission_denied]` 作为 tool_result 回传、不中断循环。附离线演示脚本。
- **改动文件**：
  - `src/agent_harness/permissions.py`（新增：Decision / PermissionRule / PermissionManager / y_n_approver）
  - `src/agent_harness/loop.py`（agent_loop 增加 `permissions` 参数,分发前判定）
  - `src/agent_harness/__init__.py`（导出权限 API）
  - `tests/test_permissions.py`（新增 6 条 mock 单测）
  - `examples/permission_demo.py`（离线演示,零 API）
- **测试结果**：`uv run pytest -q` → **11 passed**（含权限 6 条;全部 mock,零 API 消耗）。
- **commit**：`feat(permission): 新增权限白名单层(allow/ask/deny,fail-closed)`

## 2026-09-15 · 里程碑 3：接入 Git 远程仓库

- **内容**：将本地 `main` 推送到 GitHub 远程仓库 `Haaajime/HajimeCode`;确认真密钥 `.env` 未被跟踪(仅 `.env.example` 入库)。
- **改动文件**：无代码改动（仅配置了 git remote 与上游跟踪）。
- **测试结果**：`git push -u origin main` 成功，main 已跟踪 origin/main。
- **commit**：`chore(repo): 接入 GitHub 远程仓库`

## 2026-09-15 · 里程碑 4：本地仓库改名与新名统一

- **内容**：将本地仓库目录由 `agent-harness` 改名为 `HajimeCode`（与 GitHub 仓库 `Haaajime/HajimeCode` 一致）；并把源码/文档中的旧名 `agent-harness` 统一改为 `hajimecode`。
- **改动文件**：
  - 目录重命名：`agent-harness/` → `HajimeCode/`
  - `pyproject.toml`（`[project.scripts]` 入口改为 `hajimecode`；`name` 因 uv_build 要求与模块目录一致而保持 `agent-harness`）
  - `src/agent_harness/config.py`、`src/agent_harness/cli.py`（提示文案/用法改为 hajimecode）
  - `README.md`（cd 目录改为 HajimeCode、运行命令改为 hajimecode）
- **说明**：仓库名/命令名用 HajimeCode(hajimecode)；Python 包与模块目录仍为 `agent_harness`（构建约束所致，二者不冲突）。里程碑 0 中 "name=agent-harness" 为当时历史事实，保持不变。
- **测试结果**：重命名后需重新 `uv sync` 使安装包名一致；随后跑 `pytest` 确认仍为 **11 passed**。
- **运行注意事项**：当前环境存在全局 `pytest` 被 PATH 抢先的问题（`uv run which pytest` 指向 TRAE 环境 tools 目录的非存在路径），因此统一使用 **`uv run python -m pytest`** 运行测试，避免误 launch 全局 pytest。
- **commit**：`chore(repo): 本地仓库改名 HajimeCode 并统一名称引用`

## 2026-09-15 · 里程碑 5：新增"模块开发进度与优先级"文档

- **内容**：整理 `docs/开发进度与优先级.md`,按核心/机制/集成三层记录各模块状态与优先级(P0/P1/P2)、验收要点、已关闭里程碑;并在 `开发规范.md` 第 6 节纳入该文档的维护约定。
- **改动文件**：`docs/开发进度与优先级.md`（新增）、`docs/开发规范.md`（记录规范补充）。
- **测试结果**：纯文档,无代码改动。
- **commit**：`docs(progress): 新增模块开发进度与优先级文档`

## 2026-09-15 · 里程碑 6：文件工具 `glob`/`read`（P0）

- **内容**：新增只读文件系统工具,限定工作区内访问(路径 resolve 后校验仍落在 base 内,防穿越);`glob` 列出文件、`read` 按行读取并带字符软上限;接入 CLI 与包导出。
- **改动文件**：
  - `src/agent_harness/file_tools.py`（新增：WorkspaceError / _resolve_within / build_file_tools）
  - `src/agent_harness/cli.py`（注册文件工具，工作区=当前目录；SYSTEM 提示更新）
  - `src/agent_harness/__init__.py`（导出 build_file_tools）
  - `tests/test_file_tools.py`（新增 10 条单测）
- **测试结果**：`uv run python -m pytest` → **21 passed**（含文件工具 10 条；全部离线,零 API）。
- **commit**：`feat(file): 新增只读文件工具 glob/read(工作区越界防护)`

## 2026-09-15 · 里程碑 7：真实调用验证 + 修复工具 schema 校验 bug

- **内容**：用真实 DeepSeek API 跑一次带文件工具的任务（回答权限模块的 Decision 策略与默认值），验证 `glob`/`read` 端到端生效。期间发现并修复真实 bug：上游严格 schema 校验拒绝属性级 `required`（报 `true is not of type "array"`）。
- **改动文件**：
  - `src/agent_harness/tools.py`（`schema()` 剥离属性内部 `required` 标记，顶层 required 列表保留）
  - `tests/test_tools.py`（新增 4 条,其中一条针对该 schema bug 回归）
  - `src/agent_harness/__main__.py`（新增,支持 `python -m agent_harness`）
- **真实调用结果**：模型调用 `glob`/`read` 读取 `permissions.py`（100 行），正确答出 `ALLOW/ASK/DENY` 与默认 `DENY`(fail-closed)，并引用所读文件。文件工具实战可用。
- **测试结果**：`uv run python -m pytest` → **25 passed**。
- **运行注意事项**：`hajimecode`/`pytest` 命令名被全局 vm tools 目录抢先(同 pytest 问题),真实运行时统一用 **`uv run python -m agent_harness "提示词"`** 或 `.venv/bin/hajimecode`。
- **commit**：`fix(schema): 剥离属性级 required,修复上游严格 schema 校验`

## 2026-09-15 · 里程碑 8：Hooks 事件钩子（P1）

- **内容**：新增生命周期回调机制,与权限层正交。提供 `PreToolUse`(可拦截)、`PostToolUse`(观测)、`PreTurn`(每轮前注入/修改上下文)三类回调;回调异常被捕获容错,不阻断循环,安全强制仍由 PermissionManager 负责。
- **改动文件**：
  - `src/agent_harness/hooks.py`（新增：Hooks / HookAction / HookResult / run_* 执行器）
  - `src/agent_harness/loop.py`（agent_loop 增加 `hooks` 参数;pre_turn→pre_tool_use(权限后/分发前)→post_tool_use 接入）
  - `src/agent_harness/__init__.py`（导出 hooks API）
  - `tests/test_hooks.py`（新增 6 条单测）
- **测试结果**：`uv run python -m pytest` → **31 passed**（含 hooks 6 条;全部离线,零 API）。
- **commit**：`feat(hooks): 新增生命周期事件钩子(与权限层正交)`

## 2026-09-15 · 里程碑 9：Todo 计划系统（机制层 · 第 1 项）

- **内容**：新增结构化任务清单机制。提供 `update_todo` 工具(add/started/complete/revert/replace/abort),并提供一个 PreTurn hook 在每个回合把当前清单回灌进消息流,保持模型对进度的感知。
- **改动文件**：
  - `src/agent_harness/todos.py`（新增：TodoList / TodoStatus / build_todo_system）
  - `src/agent_harness/__init__.py`（导出 todo API）
  - `tests/test_todos.py`（新增 7 条单测）
- **测试结果**：`uv run python -m pytest` → **38 passed**（全部离线,零 API）。
- **commit**：`feat(todo): 新增结构化任务清单与进度回灌`