# 开发日志（Agent Harness 项目）

> 追加式记录。每完成一项工作在此追加一条 [日期] / 内容 / 改动文件 / 测试结果 / commit。
> 与 `开发规范.md` 配合，遵循"完成一项 → 记日志 → git commit"。

---

## 2026-09-15 · 里程碑 0：项目初始化与工程建立

- **内容**：使用 `uv` 初始化项目，确定 Python 版本与依赖管理方式；建立敏感信息与日志/规范文档体系。
- **改动文件**：
  - `pyproject.toml`（uv 建包，name=agent-harness）
  - `.python-version`（钉到 3.10，规避网络下载 3.11 超时）
  - `.env`（含 DeepSeek key，git 已忽略）、`.env.example`（占位）
  - `.gitignore`（加入 `.env` 忽略规则）
  - `docs/开发规范.md`、`docs/开发日志.md`
- **测试结果**：无（纯工程搭建）。
- **commit**：`chore(init): 项目初始化,树立开发规范与日志体系`

## 2026-09-15 · 里程碑 1：核心引擎（循环 + 工具注册表 + Provider）

- **内容**：实现 Agent 主循环、工具分发注册表、模型 Provider 抽象（DeepSeek OpenAI 兼容）与 CLI 入口。
- **改动文件**：
  - `src/agent_harness/config.py`（.env 配置加载）
  - `src/agent_harness/providers.py`（OpenAICompatibleProvider）
  - `src/agent_harness/tools.py`（Tool + ToolRegistry + 内置 add/echo）
  - `src/agent_harness/loop.py`（agent_loop：tool_use 分发 + tool_result 回填 + 步数上限）
  - `src/agent_harness/cli.py`、`src/agent_harness/__init__.py`、`pyproject.toml`（脚本入口）
  - `tests/test_loop.py`（5 条 mock 单测）
  - `README.md`
- **测试结果**：`uv run pytest -q` → **5 passed**（全部 mock，零 API 消耗）。
- **真实 API 冒烟**：一次极小 token 调用 DeepSeek（max_tokens=120）返回"联通"，端到端打通。
- **commit**：`feat(core): 实现 Agent 主循环、工具注册表与 DeepSeek Provider`

## 2026-09-15 · 里程碑 2：权限治理层（s03）

- **内容**：新增权限判定层,在工具执行前做 `allow/ask/deny` 判定,默认 fail-closed(未命中放行规则即拒绝),并接入主循环:拒绝以 `[permission_denied]` 作为 tool_result 回传、不中断循环。附离线演示脚本。
- **改动文件**：
  - `src/agent_harness/permissions.py`（新增：Decision / PermissionRule / PermissionManager / y_n_approver）
  - `src/agent_harness/loop.py`（agent_loop 增加 `permissions` 参数,分发前判定）
  - `src/agent_harness/__init__.py`（导出权限 API）
  - `tests/test_permissions.py`（新增 6 条 mock 单测）
  - `examples/permission_demo.py`（离线演示,零 API）
- **测试结果**：`uv run pytest -q` → **11 passed**（含权限 6 条;全部 mock,零 API 消耗）。
- **commit**：`feat(permission): 新增权限白名单层(allow/ask/deny,fail-closed)`